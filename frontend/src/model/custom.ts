export type Color = {
  background: string;
  color: string;
};

export type Border = {
  radius?: number;
  borderColor: string;
  border: number;
};

export type Font = {
  fontSize: number;
  fontStyle?: string;
  fontWeight?: number;
};

export type Size = {
  height : number;
  width: number;
}